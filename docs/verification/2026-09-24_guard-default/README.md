# 守りの既定変更（2026-09-24）の独立の確認

変更: 「聞く」は既定で承認画面を出さず止める（`HARNESS_ATTENDED=1` のときだけ聞く。人が OK した操作は先頭の `HARNESS_APPROVED=1` で 1 回だけ通す）。

確認した担い手: GPT-6 Sol（openai。作った側は Claude Fable 5.1 / anthropic）。実装を読む前に対を 17 件書いて保存し（hash を報告に記録）、それから測った。

結果: **採用。** 17 件中 16 件が一致。抜け道 5 通り（`HARNESS_APPROVED=1;`、`export`、`bash -c`、`;` でつないだ 2 つ目、`env` で main へ push）は全部止まった。守りを記憶上で 3 通り壊すと、事前の対で 3 通りとも検出できた。

不一致 1 件の裁定: `HARNESS_APPROVED=1 rm -rf ./tmp`（作業木の中の再帰削除）を担い手は deny と期待したが、実測は allow。**これは設計どおり**（守りが止めるのは「作業場の外・作業場全体・`.git` を木ごと消す」で、作業場の中の決め打ちの path の削除は通す。`tests/guard_cases.tsv:14-15`）。依頼書の「壊す命令」の言い方が曖昧だった。指示役の裁定で、実装は変えない。
