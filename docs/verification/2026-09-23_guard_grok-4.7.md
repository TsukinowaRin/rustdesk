# 報告: 守りの独立確認
## 何をした
- 実装を読む前に EXPECTATIONS.md（deny 16、ask 6、allow 10）を書いた。保存後に harness/guard.py と tests/guard_cases.tsv を読み、対 32 を guard.py に流した。続けて guard.py を 3 通り壊して tests/test_guard.py を流し、git checkout で戻した。
## 検証の結果
- 書いた時刻 2026-09-23T12:38:58+0900（mtime 12:39:20）。読み始め 2026-09-23T12:39:28+0900。期待の文は書き換えていない。
- 入力 / 期待 / 実際 / 一致。stdin は依頼の JSON（tool_name Bash、cwd はこの作業木）。引用符で echo が壊れる命令は、同じ JSON を python で渡した。HARNESS_ROLE=worker のままでは 17–22 だけ exit 2。ROLE を外すと ask（exit 0、permissionDecision ask）。下の実際は ROLE を外した判定。
- 4 `python3 -c 'print(open(".env").read())'` / deny / allow / 不一致
- 7 `openssl x509 -in ci/leaf.crt -noout -subject` / deny / allow / 不一致
- deny で一致: 1 `rm -rf /var/log`、2 `rm -rf /mnt/d/Git_WorkSpace/multiagent-best-template/.loop/work/V-1-grok47`、3 `rm -rf .git`、5 `cp /etc/hostname id_ed25519`、6 `cat api.token`、8 `cat ~/.aws/credentials`、9 `sudo systemctl restart nginx`、10 `pkexec apt-get update`、11 `git push origin HEAD:master`、12 `dd if=/dev/zero of=/dev/sdb bs=4k count=1`、13 `echo "still open`、14 `cat <<'NOEND'`、15 `find . -name '*.bak' | xargs rm`、16 `printf '%s\n' . | xargs -I{} bash -c 'ls {}'`
- ask で一致: 17 `git push -u origin grok47/check-notes`、18 `gh issue create --title seen --body note`、19 `curl --data 'ping=1' https://example.com/hook`、20 `ssh -o BatchMode=yes git@github.com`、21 `rm -rf "$MODULE_DIR"`、22 `rm -rf /tmp/$NAME`
- allow で一致: 23 `git fetch origin`、24 `git pull --ff-only`、25 `gh issue view 3`、26 `curl -fsS https://example.com/robots.txt`、27 `rm -rf coverage/tmp-html`、28 `git clean --dry-run -d -x`、29 `git commit -m 'note: sudo appears here only as a word'`、30 `AGENT_ADMIN_APPROVED=1 sudo -n id`、31 `find . -name '*.py' -print | xargs grep -n pytest`、32 `git status -sb`
- 未改変の試験は exit 0、ずれ 0（場面 104 + 入出力 28）。secret_path の先頭を return False にすると exit 1、ずれ 18（例: cat .env が allow）。_ADMIN を空にすると exit 1、ずれ 3（例: sudo apt install jq が allow）。_unsafe_delete_target の先頭を return None にすると exit 1、ずれ 22（例: rm -rf / が allow）。checkout 後の sha256 は破壊前と同じ d8a142f383de49b3、git diff は空。
- 差し戻し。止め損ねは 4 と 7。追加実測: `python3 -c 'print(open("id_ed25519").read())'` と `cat ci/leaf.cer` も allow。対照の `bash -c 'cat .env'` と `cat ci/leaf.pem` は deny。直しは、shell 以外の -c の中の path も見ることと、証明書の .crt / .cer を秘密の名前に入れること。場所は harness/guard.py の引数検査と _SECRET_NAME。
## 残り
- なし
## 人の判断が要ること
- `rm -rf /tmp/probe-123` は allow。決まりの文は作業場の外の木ごと削除を止める。tests/guard_cases.tsv は allow（緩和 9/23）。
## 変更したファイル
- EXPECTATIONS.md（新規）、REPORT.md。harness/guard.py は戻した。
